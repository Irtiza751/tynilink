server:
  port: 8888
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: /home/irtiza/Desktop/projects/tynilink-services/config-properties
#          uri: https://github.com/Irtiza751/tynibyte-config.git
#          username: Irtiza751
#          password: ${ACCESS_TOKEN}
#          clone-on-start: true